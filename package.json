{
  "name": "@prng/core",
  "version": "1.0.1",
  "description": "A Collection of PRNG's in JavaScript and TypeScript",
  "main": "prng.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rimraf *.js *.d.ts"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.5.4"
  }
}
